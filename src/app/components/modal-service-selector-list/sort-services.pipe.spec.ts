import { SortServicesPipe } from './sort-services.pipe';

describe('SortServicesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortServicesPipe();
    expect(pipe).toBeTruthy();
  });
});
